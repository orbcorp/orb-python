#!/usr/bin/env bash

set -eux
rm -rf dist
mkdir -p dist
uv build
# No token: uv detects PyPI Trusted Publishing (GitHub Actions OIDC) when the
# workflow has id-token: write and the publisher is registered on the project.
uv publish --trusted-publishing always
